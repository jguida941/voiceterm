# Tests

Scratch and standalone script tests that are not part of the `devctl` package.

- Keep one-off helper or stress scripts here.
- `dev/scripts/devctl/tests/` remains the canonical unit-test home for Python tooling.
