# Mutation

Canonical home for mutation-testing CLI logic and helpers.

- `cli.py`: canonical mutation CLI entrypoint.
- `config.py`: module registry and shard parsing.
- `git.py`: changed-file targeting.
- `runner.py`, `results.py`, `plot.py`: execution, parsing, and visualization helpers.
