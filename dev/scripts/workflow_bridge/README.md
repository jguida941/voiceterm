# Workflow Bridge

Canonical home for CI-facing shell and workflow adapters.

- `autonomy.py`: autonomy-controller and Ralph workflow config/export helpers.
- `shell.py`: shared workflow shell helpers for commit ranges, coverage, and
  artifact plumbing.
- `mutation_ralph.py`: mutation Ralph workflow entry logic.
- Supporting modules in this directory stay reusable and workflow-focused.
