# Devctl Tests

Canonical unit-test home for Python tooling under `dev/scripts/devctl/`.

- Keep test modules mirrored to the package or command they cover.
- Path-based wrapper tests can still target compatibility entrypoints when needed.
