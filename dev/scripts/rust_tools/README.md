# Rust Tools

Canonical home for Rust workflow helper scripts.

- `collect_clippy_warnings.py`: Clippy JSON summary helper for local and CI
  lint histograms.
- `dependency_graph_probe.py`: dependency graph availability probe for
  workflow gating.
