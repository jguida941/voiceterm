# Post-v1.0.64 Audit (2026-02-16)

## Scope

- Covered releases: `v1.0.65` through `v1.0.68`
- Focus: workflow parity, mutation gate health, macro onboarding UX, and docs sync

## Automated Audit Results

| Check | Command | Result |
|---|---|---|
| CI parity | `python3 dev/scripts/devctl.py check --profile ci` | PASS |
| Pre-push parity | `python3 dev/scripts/devctl.py check --profile prepush` | PASS |
| Parser fuzz lane parity | `make parser-fuzz` | PASS |
| Mutation threshold | `python3 dev/scripts/devctl.py mutation-score --threshold 0.80` | PASS (`89.87%`) |
| Traceability guard parity | `python3 dev/scripts/check_audit_traceability.py` | PASS |
| Docs drift gate | `python3 dev/scripts/devctl.py docs-check --user-facing` | PASS |
| Governance hygiene | `python3 dev/scripts/devctl.py hygiene` | PASS (1 warning: existing `__pycache__` dirs) |
| Release binary build | `cd src && cargo build --release --bin voiceterm` | PASS |

## Macro/Wizard Hardening Verified

- Added/validated macro packs:
  - `scripts/macro-packs/safe-core.yaml`
  - `scripts/macro-packs/power-git.yaml`
  - `scripts/macro-packs/full-dev.yaml`
- Wizard validated:
  - `./scripts/macros.sh list`
  - `./scripts/macros.sh install --pack safe-core --project-dir . --output /tmp/voiceterm-macros-safe.yaml --overwrite`
  - `./scripts/macros.sh install --pack power-git --project-dir . --output /tmp/voiceterm-macros-power.yaml --overwrite`
  - `./scripts/macros.sh install --pack full-dev --project-dir . --output /tmp/voiceterm-macros-full.yaml --overwrite`
  - `./scripts/macros.sh validate --output /tmp/voiceterm-macros-full.yaml --project-dir .`
  - `printf '%s\n' '.' '/tmp/voiceterm-macros-wizard-preselect.yaml' '' '' '' | ./scripts/macros.sh wizard --pack full-dev`
- Placeholder auto-fill now supports:
  - `__GITHUB_REPO__`
  - `__GITHUB_OWNER__`
  - `__DEFAULT_BRANCH__`
  - `__GITHUB_USER__`
  - `__CURRENT_BRANCH__`
- GitHub macro packs now validate:
  - `gh` binary availability
  - `gh auth status` readiness
  - repo access via `gh repo view <owner/name>` when repo is known
- Source install flow now offers macro onboarding:
  - interactive prompt after `./scripts/install.sh`
  - explicit flags: `--with-macros-wizard`, `--macros-pack`

## Notes / Gaps

- Live GitHub Actions query failed in this environment (`api.github.com` connectivity error), so remote workflow status could not be confirmed from this run.
- Physical UX verification remains required on a real terminal/audio session (voice capture, status transitions, navigation phrases, clipboard behavior, restart/reset).
