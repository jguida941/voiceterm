# Automation Debt Register

Track repeated manual friction that should become guarded automation.

Rule: if a workaround repeats 2+ times in the same MP scope, either automate it
or log it here with a closure plan.

## Open Items

| ID | First seen | MP scope | Repeated manual step | Repeat count | Risk | Current guard path | Automation target | Owner | Status | Exit criteria |
|---|---|---|---|---:|---|---|---|---|---|---|
| ADR-001 | 2026-02-24 | `MP-330..MP-331` | Manual phone-adapter endpoint smoke flow not yet consolidated in a single command | 0 | medium | Baseline audit checklist + explicit manual script | Add `devctl` mobile-control smoke runner with report bundle | unassigned | open | CI/local command executes full endpoint smoke and emits pass/fail artifact |
| ADR-002 | 2026-03-18 | `MP-377` | Manual Codex/Claude bridge polling + re-review loop repeatedly stalls on stale-hash/heartbeat churn instead of promoting next bounded slice | 6 | high | `review-channel reviewer-heartbeat --follow`, `check_review_channel_bridge.py`, manual `bridge.md` reviewer rewrites | Add one repo-owned `devctl` reviewer-loop action that performs poll -> focused re-review -> bridge checkpoint update -> next-slice promotion (or debt/backlog handoff) in one guarded path | `MP-377` lane (Codex conductor) | open | Single command can run the full review loop without manual bridge babysitting and demonstrates deterministic next-slice promotion within 2 consecutive cycles |
| ADR-003 | 2026-04-08 | `MP-377` / `MP-380..MP-387` | Manual remote-lane proofing still requires ad hoc stage/approve/commit/push checks and live runtime babysitting to confirm the typed path survives reviewer death or fails closed correctly | 2 | high | Focused pytest bundles, `startup-context`, `check_review_channel_bridge.py`, and live `review-channel status/doctor` inspection | Add one repo-owned `devctl` remote-lane smoke harness that runs stage -> guard -> approve -> commit -> push against the typed pipeline, kills the reviewer/runtime after approval, and emits a typed pass/fail recovery report | `MP-377` lane (governed VCS + remote control) | open | One local/CI command proves the governed remote lane either completes publish through typed state or returns a typed recover action with no raw shell escape |
| ADR-004 | 2026-04-22 | `MP-377` / `MP-380..MP-387` | Operator has to manually notice local Codex approval prompts while a Claude remote-control dashboard is active; prompts are not automatically mirrored to the typed remote-control lane | 2 | high | `startup-context` summary, `review-channel status`, `stage_commit_pipeline` action-request dogfood, operator screenshots, and `operator-inbox` packet checks | Route or mirror operator-action prompts into typed `action_request` / approval packets whenever an attached remote-control session is live, while keeping local terminal output as a secondary echo | `MP-377` lane (remote control + governed VCS) | open (commit-stage adapter shipped) | With a live remote-control attachment, startup/status report co-present local + remote-control topology and any command requiring operator action appears in the remote dashboard/operator inbox without manual copy/paste |
| ADR-005 | 2026-04-22 | `MP-377` / `MP-387` | Role-filter fixes land on one advisory surface while sibling commands still emit implementer-lane next actions to observer/dashboard callers | 1 | medium | `rev_pkt_1647`, `session-resume --role observer`, `startup-context --role observer`, dashboard dogfood runs, shared `advisory_next_action_role_filter.py` focused tests | Add `probe_advisory_next_action_role_filter.py` and keep caller-role filtering threaded through startup-context, dashboard, session-resume, authority, and control-plane advisory-next-action surfaces | `MP-377` lane (runtime authority + dashboard) | open | Probe finds all advisory-next-action surfaces, observer calls never receive mutating next commands, and dashboard accepts/derives observer role when remote-control dashboard mode is active |
| ADR-006 | 2026-04-22 | `MP-377` / `MP-390` | Long governed operations require manual packet-watcher fanout so Claude-to-Codex findings are not missed while tests/guards run | 1 | medium | `rev_pkt_1648`, manual Hypatia watcher spawn, review-channel inbox polling | Add a typed long-op watcher contract or advisory probe that marks governed commands expected to exceed five minutes and requires packet-watcher coverage | `MP-377` lane (auto-mode + review-channel runtime) | open | Long commit/push/test flows emit watcher coverage metadata or an advisory finding, and packet queues are checked automatically before and after the long operation |
| ADR-009 | 2026-04-22 | `MP-377` / `MP-380..MP-390` | Post-commit publish readiness is manually composed from pipeline status, startup-context, review-channel status, review-surface consistency, and push | 6 | high | `rev_pkt_1655`, repeated post-commit checklist runs, push authorization receipts | Add `devctl pipeline --action finalize-or-publish` or equivalent typed action that verifies commit-recorded state and performs the governed push when authorized | `MP-377` lane (governed VCS pipeline) | open | A recorded commit can move to published state through one typed action with a verification receipt and clear blocker packet on failure |
| ADR-011 | 2026-04-26 | `MP-377` / `MP-376` | Guard violations still rely on each guard or operator to decide whether to write a governance-review row | 2 | high | `check --profile ...`, `check-router`, `governance-review --record`, default report-only failed-command dogfood ingest | Route structured guard failures through the shared `PlatformFindingIngest` helper in warning/report-only mode before any blocking enforcement | `MP-377` lane (guard governance) | open (dogfood command-failure spine closed by ADR-010; per-guard routing pending) | Guard failures produce one canonical FindingBacklog row per stable finding identity with no bespoke guard writers |
| ADR-012 | 2026-04-26 | `MP-377` / `MP-390` | Packet outcomes discovered after commits are classified by manual history review instead of a repo-managed post-commit classifier | 2 | medium | `review-channel --action history --include-outcomes`, manual packet review | Extend the managed post-commit hook template with a warning-only packet classifier after ReviewSnapshot receipt refresh | `MP-377` lane (packet lifecycle) | open | Post-commit runs classify stale wait/missing ACK/delivered-by-commit outcomes and can promote unresolved cases through the finding-ingest seam |
| ADR-013 | 2026-04-26 | `MP-377` / `MP-380..MP-390` | Current runtime unblock still requires hand-running startup-context, review-channel status/doctor, operator inbox/history, and then manually comparing authority before push | 2 | high | startup-context, review-channel status/doctor/operator-inbox/history, dashboard, push preflight | Add one read-only runtime-agreement diagnostic that runs the Slice 0 command chain and emits one typed agreement report for Codex/Claude/dashboard/push state | `MP-377` lane (runtime agreement) | open | One command proves runtime authority, packet state, review state, and publication state agree before governed push |
| ADR-015 | 2026-04-26 | `MP-377` / Plan 4.1 Slice A | Findings discovered during dogfood/push/test/debug sessions can be ACKed in packets but not durably ranked and surfaced without pulling Codex off the active slice | 1 | high | `FindingBacklog`, `governance-review`, `findings-priority`, `AutoModeState`, dashboard, `startup-context.quality_signals`, `review-channel --kind finding` | Extend `PlatformFindingIngest` so packet findings and ad-hoc captures mirror into the existing finding spine with severity, plan anchor, evidence ref, terminal outcome, stale-pending counters, dashboard/startup visibility, and non-distraction slice-boundary ordering | `MP-377` lane (finding ingest) | open | A finding captured through review-channel or `devctl finding capture` remains visible in dashboard/startup/findings-priority until explicitly applied, dismissed, superseded, or promoted, and non-critical off-slice findings do not interrupt active slice work |
| ADR-016 | 2026-04-26 | `MP-377` / Plan 4.1 Slice A-B | Codex implementer sessions do not continuously consume Codex-targeted packets, so Claude findings can land while bounded Codex sessions remain unaware | 1 | high | `review-channel inbox/history`, `PacketOutcomeLedger`, dogfood ledger, `AutoModeState` | Add `CodexAgentPollLoop` as an existing review-channel consumer: poll Codex inbox on cadence, ACK/route findings, preflight action_requests, emit dogfood rows, and let the Slice B lifecycle reducer write terminal outcomes such as `superseded_by` | `MP-377` lane (review-channel runtime) | open | In a fixture where Claude posts a finding and action_request after a Codex session starts, Codex consumes them within one tick, updates or refuses through typed packets, and no packet remains pending indefinitely |
| ADR-017 | 2026-04-26 | `MP-377` / Plan 4.1 Slice 0-B-E | Phone remote-control operation still requires the operator to launch local terminals or flip reviewer modes manually when automation_loop already has scoped authorization | 1 | critical | `permission_policy.json`, caller-class policy, `agent_lane.permissions`, `review-channel --action launch`, `remote_control_attachment` | Extend existing caller-class and launch/recover typed contracts so automation_loop can perform scoped headless launch, recover, mode-switch, and `spawn_codex_implementer` requests with operator-scope stamps, without adding a parallel automation launcher | `MP-377` lane (remote control) | open | A typed `spawn_codex_implementer` action_request from automation_loop with an operator-scope stamp spawns Codex headlessly and records CommandRequest/CommandResult evidence without a local TTY prompt |
| ADR-019 | 2026-04-27 | MP-377 / Plan 4.1 Slice A-C | check_contract_connectivity.py finds orphans but in advisory-only mode; 25+ stranded consumers logged without blocking CI | 1 | critical | check_contract_connectivity.py (warning-only), check_platform_contract_closure.py (registered-only) | Promote check_contract_connectivity to fail-on-stranded with CONNECTIVITY_ENFORCEMENT_MODE env var (warning_only / fail_on_new_orphan / fail_on_any_stranded); use Slice C retirement-rule to clear baseline before flipping default | MP-377 lane (governance closure) | open | check_contract_connectivity.py fails CI on new orphans by default after baseline retirement; surfaces 0 new stranded consumers in routed CI |

## Closed Items

| ID | Closed date | MP scope | Resolution | Evidence |
|---|---|---|---|---|
| ADR-007 | 2026-04-22 | `MP-377` / `MP-380..MP-390` | Added `devctl pipeline --action auto-recover`, a typed classifier/dispatcher that selects recover, refresh-authorization, abandon, no-op, or fail-closed ambiguous and writes `PipelineAutoRecoveryReceipt` for audit. | `dev/scripts/devctl/commands/pipeline/auto_recover_action.py`, `dev/scripts/devctl/runtime/pipeline_auto_recovery_contracts.py`, `dev/scripts/devctl/tests/commands/test_pipeline_command.py` |
| ADR-008 | 2026-04-23 | `MP-377` / `MP-387` | Added managed projection receipt commits inside `devctl push`: after preflight reprojects `bridge.md`, bridge-only or receipt-artifact-only drift is committed as the governed external-review receipt before publication authorization, pipeline sync accepts that receipt HEAD as bound to the approved content commit, pipeline status/auto-recover classify the trailing receipt HEAD as `managed_receipt`, and the ReviewSnapshot freshness guard now accepts snapshots bound to any ancestor in the contiguous managed receipt chain while push preflight runs the snapshot receipt refresh when that batch moves `HEAD`. | `dev/scripts/devctl/commands/vcs/push_projection_receipt.py`, `dev/scripts/devctl/commands/vcs/push_preflight_projection.py`, `dev/scripts/checks/review_snapshot_freshness/command.py`, `dev/scripts/devctl/tests/vcs/test_push.py`, `dev/scripts/devctl/tests/checks/test_check_review_snapshot_freshness.py`, `dev/scripts/devctl/tests/commands/test_pipeline_command.py` |
| ADR-010 | 2026-04-27 | `MP-377` / Plan 4.1 Slice A | Promoted the `PlatformFindingIngest` finalization hook to default fail-open report-only recording for failed non-read-only devctl commands. The hook now appends a `DogfoodRun` row plus a stable `signal_type=dogfood` governance-review/FindingBacklog row after audit emission, refreshes summaries, excludes read-only/recursive/artifact-only commands, and keeps `DEVCTL_PLATFORM_FINDING_INGEST_AUTO_RECORD=0` plus `DEVCTL_PLATFORM_FINDING_INGEST_DISABLE=1` as opt-out/kill-switch controls. | `dev/scripts/devctl/runtime/platform_finding_ingest.py`, `dev/scripts/devctl/runtime/dogfood_render.py`, `dev/scripts/devctl/tests/runtime/test_platform_finding_ingest.py` |
| ADR-014 | 2026-04-26 | `MP-377` / Plan 4.1 Slice 0 | Remote-control recover could recommend and launch a local Terminal.app Claude prompt that the attached remote operator would not see. Status authority now prefers typed remote-control liveness over local governance defaults, and launcher discipline refuses visible Terminal.app launches in `remote_control`. | `dev/scripts/devctl/review_channel/status_snapshot_authority.py`, `dev/scripts/devctl/commands/review_channel/launcher_discipline.py`, `dev/scripts/devctl/tests/review_channel/test_status_snapshot_authority.py`, `dev/scripts/devctl/tests/review_channel/test_launch_script.py` |
| ADR-018 | 2026-04-27 | `MP-377` / Plan 4.1 Slice 0 | Repeated push attempts exposed that policy-owned render-surface drift, especially `SYSTEM_MAP.md`, was still a manual repair even after bridge/ReviewSnapshot receipt automation. `devctl push` now runs `render-surfaces --write` before routed preflight, commits tracked non-local render targets as pathscoped generated-surface receipts, accepts those receipts in the managed chain above the authorized content commit, and preserves staged-only next-commit intent outside the receipt. | `dev/scripts/devctl/commands/vcs/push_preflight_projection.py`, `dev/scripts/devctl/commands/vcs/push_render_surface_sync.py`, `dev/scripts/devctl/commands/vcs/push_projection_runtime_refresh.py`, `dev/scripts/devctl/commands/vcs/push_preflight_commit.py`, `dev/scripts/devctl/commands/vcs/push_projection_receipt.py`, `dev/scripts/devctl/runtime/review_snapshot_refresh.py`, `dev/scripts/devctl/tests/vcs/test_push.py`, `dev/scripts/devctl/tests/runtime/test_push_authorization.py` |
