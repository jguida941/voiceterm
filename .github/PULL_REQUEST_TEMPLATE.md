## Summary
What does this change do and why?

For Theme Studio scope (MP-148..MP-182), use:
`.github/PULL_REQUEST_TEMPLATE/theme_studio.md`

## Testing
- [ ] `cd src && cargo build --release --bin voiceterm`
- [ ] Other (describe):

## Checklist
- [ ] Docs updated if user-facing behavior changed
- [ ] `dev/CHANGELOG.md` updated if user-facing behavior changed
- [ ] Screenshots/controls tables updated if UI output or flags changed
